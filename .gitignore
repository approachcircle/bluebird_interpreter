# precompiled binaries
/bin

# auto compilation files (a copy of this command will be in the readme anyway)
compile.bat

# i'm lazy okay
start.bat

# crash dumps, shouldn't be distrubuted
dump.dat

# vscode workspaces
*.code-workspace