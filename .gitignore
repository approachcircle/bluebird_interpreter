# precompiled binaries
bin/*.exe

# auto compilation files etc (a copy of the compile command in one of these batch files will be in the readme anyway)
*.bat

# crash dumps, shouldn't be distrubuted
dump.dat

# vscode workspaces
*.code-workspace

# files for testing new bluebird functions
testing/